---
title: Interactive Examples
description: Interact with a counter, task list, and persisted preferences powered by ZhuàngTài and React.
tableOfContents: false
sidebar:
  label: Interactive Examples
  order: 3
---

import InteractiveExamples from "../../../components/InteractiveExamples.astro";

## Try it before the API

Click a button, add a task, or change a preference and the interface updates immediately. These examples run the actual <code className="whitespace-nowrap">@zhuangtai-js/core</code>, <code className="whitespace-nowrap">@zhuangtai-js/react</code>, and <code className="whitespace-nowrap">@zhuangtai-js/persist</code> packages, so the behavior matches real application code.

<InteractiveExamples locale="en" />

## Example takeaways

- **State can live outside components**: multiple components can read one atom without an extra Provider.
- **React integration stays familiar**: `useAtom` feels close to `useState`, while state is not bound to one component lifecycle.
- **Derived values need no dependency array**: `computed` tracks the atoms read by the current evaluation.
- **Objects and arrays use immutable updates**: the task list creates new arrays and objects when it changes.
- **Capabilities compose on demand**: only the preferences example adds `persist`, while Core stays lightweight.

## Start with React

```sh
pnpm add @zhuangtai-js/core @zhuangtai-js/react
```

```tsx
import { atom } from "@zhuangtai-js/core";
import { useAtom } from "@zhuangtai-js/react";

const count = atom(0);

export function Counter() {
  const [value, setValue] = useAtom(count);
  return <button onClick={() => setValue((n) => n + 1)}>{value}</button>;
}
```

Continue with the [React guide](/en/guides/react/) or open the [complete examples](/en/examples/).
