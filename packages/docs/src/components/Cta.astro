---
---

<div class="cta-section">
  <slot />
</div>

<style>
  .cta-section {
    margin-top: 4rem;
    padding: 3rem 1rem;
    text-align: center;
    background: var(--sl-color-gray-6);
    border-radius: 1rem;
    border: 1px solid var(--sl-color-hairline);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.5rem;
  }

  :global(.cta-section h2) {
    margin: 0;
    font-size: var(--sl-text-h2);
  }

  :global(.cta-section > p) {
    margin: 0;
    color: var(--sl-color-gray-3);
    max-width: 40ch;
  }

  :global(.cta-section .action-group) {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
    justify-content: center;
    margin-top: 0.5rem;
  }
</style>
